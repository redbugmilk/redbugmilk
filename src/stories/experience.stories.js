import React from 'react';
import Experience from '../controllers/Experience';

export default {
    title: 'Experience wrapper',
    component: Experience,
};

const Template = () => <Experience />;

export const ExperienceComponent = Template.bind({});
